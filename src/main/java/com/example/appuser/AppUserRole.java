package com.example.appuser;

public enum AppUserRole {       // enums:-Enums are used when we know all possible values at compile time,
         USER,                      // such as choices on a menu, rounding modes, command line flags, etc.
        ADMIN                             // It is not necessary that the set of constants in an enum type stay fixed for all time.
                                     //  In Java (from 1.5), enums are represented using enum data type.

}
